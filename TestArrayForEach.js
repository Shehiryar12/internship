// const numbers=[30,40,50,65,70]
// numbers.forEach((num)=>console.log(num))


// Use forEach to find the sum of all numbers in the array:

let sum=0
 const numbers = [5, 10];
 numbers.forEach( (num , index) => {
          sum =sum+ num
 } )
 console.log(sum)
